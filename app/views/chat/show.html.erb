<div class='container px-5 px-sm-0'>
  <div class="col-md-8 mx-auto">

    <p class="text-center bg-light py-3 mb-5">ようこそ<%= current_user.name %>さん！<br>
    ここでは<%= @master.name %>さんと自由にお話が出来ます。</p>


    <div id="chat_list">
        <%= render 'chat_list', chats: @chats %>
    </div>

    <div id="chat_form" class="form mt-5">
      <%= render 'chat_form', new_chat: @new_chat %>
    </div>

  </div>
</div>
